spring:
  application:
    #name: organizationservice
    #name: customerservice
    name: customer-service
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888
      
 